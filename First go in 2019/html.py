# import requests

'http://docs.python-requests.org/en/master/'

# varijabla = requests.get(url)

# varijabla.status_code
# varijabla.text

# requests.get(url, headers{"header1": "value1", ...})
# varijabla.json()     - turns the string into python

# headers={"Accept": 'text/plain'}
#                    'text/html'      - hrpetina teksta kao i .text
#                    'appplication/json' - plain text, gives out string which we turn into python code
# params={'key1': 'value1}, ...}